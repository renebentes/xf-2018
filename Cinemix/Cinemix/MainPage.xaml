<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Cinemix.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <ScrollView>
        <StackLayout>
            <StackLayout>
                <Label Style="{StaticResource PageTitle}" Text="Cinemix" />
                <Label Style="{StaticResource PageSubtitle}" Text="Seu app de cinema" />
            </StackLayout>
            <StackLayout Style="{StaticResource ContentLayout}">
                <Label Text="Lançamento" />
                <Label Style="{StaticResource MovieTitle}" Text="Pantera Negra" />

                <Grid>
                    <Image
                        Source="{OnPlatform Default=fundo.png,
                                            UWP=Assets/fundo.png}" />
                    <Image
                        Margin="0,15,0,0"
                        Source="{OnPlatform Default=poster.png,
                                            UWP=Assets/poster.png}"
                        VerticalOptions="Start" />
                </Grid>
            </StackLayout>
        </StackLayout>
    </ScrollView>
</ContentPage>
